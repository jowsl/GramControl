#ifndef TEST_PERSISTENCIA_H
#define TEST_PERSISTENCIA_H

// Testa se o UPDATE no banco afeta a linha corretamente
/**
 * @brief Testa a persistência de dados no banco de dados.
 *
 * Esta função verifica se a operação de atualização (UPDATE) no banco de dados afeta corretamente a linha desejada,
 * garantindo que os dados sejam persistidos de forma adequada.
 */
void teste_persistencia_update_afeta_linha();

#endif // TEST_PERSISTENCIA_H